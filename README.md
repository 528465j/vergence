# vergence
Vergence: a controls-first pipeline that reconciles multiple independent data sources across inconsistent provider schemas. Deterministic by default; LLM-assisted column mapping behind a human approval gate.
